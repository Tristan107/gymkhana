# Plan: Resolve SonarQube issues (sonar_issues.json)

Source export: `sonar_issues.json` (generated by `sonar-scan.sh`, gitignored).
Goal: fix the 2 legitimate findings and mark the 1 false positive as reviewed/won't-fix.

## Issue 1 — S3776: reduce Cognitive Complexity of `openingMoves` (CRITICAL)

- File: `src/game/ai_v2.ts`, line 686 (`openingMoves`), complexity 18 > 15.
- Cause: red/blue branch each containing a loop, plus a `moves.filter` with a nested `for...of DIRS`.
- Fix (behavior-preserving refactor only):
  - Extract a helper, e.g. `function edgeTokens(board: Board, player: Player): Set<string>` that collects the player's tokens on their two home-lane edges (the current lines 688–698 logic, keyed as `"row,col"` strings).
  - `openingMoves` then becomes: build the set via the helper, filter moves excluding home-lane moves (`onHomeLane`) and keeping moves adjacent to an edge token.
  - No changes to move selection semantics; `pickRandom` tie-breaking stays.
- Constraints:
  - AI spec lives in `.opencode/01_simplified_rules_for_ai.md` / `.opencode/02_basic_heuristics.md` — verify opening-move behavior is untouched.
  - Verify with `npm run test:ai` (ai_v2.test.ts) and `npm test`.

## Issue 2 — S6819: backdrop div with `role="button"` in DevMenu (MAJOR)

- File: `src/components/DevMenu.tsx`, line 45–54.
- Problem: modal backdrop uses `role="button"`, but has `tabIndex={-1}` so the `onKeyDown` handler is dead code; a backdrop is not a button.
- Fix:
  - Remove `role="button"`, `tabIndex={-1}`, `aria-label`, and the `onKeyDown` handler from the backdrop div.
  - Keep `className` and `onClick={onClose}` (click-to-dismiss still works).
- Note: no Escape-key handling exists elsewhere in this dialog; adding one is out of scope unless requested.

## Issue 3 — S2245: `Math.random()` in `pickRandom` (MAJOR, VULNERABILITY) — false positive

- File: `src/game/ai_v2.ts`, line 860–862.
- Context: used only to break ties among equally-scored AI moves (`src/game/ai_v2.ts:33` and `:857`). No security-sensitive use.
- Action: no code change. Mark the issue as **Won't Fix** ("safe: PRNG used only for non-security tie-breaking in game AI") in SonarQube UI, or add a `// NOSONAR` comment if marking server-side isn't practical. Default: mark in SonarQube UI to keep code clean.

## Verification

1. `npm run lint`
2. `npm run build`
3. `npm test`
4. `npm run test:ai`
5. Optional: re-run `./sonar-scan.sh` against local SonarQube and confirm issues 1–2 are resolved and issue 3 shows as Won't Fix.

## Files touched

- `src/game/ai_v2.ts` (refactor `openingMoves`; extract edge-token helper)
- `src/components/DevMenu.tsx` (simplify backdrop)
